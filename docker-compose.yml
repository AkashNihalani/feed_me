services:
  feedme-worker:
    build:
      context: ./apps/worker
      dockerfile: Dockerfile
    container_name: feedme-worker
    restart: always
    env_file:
      - ./apps/worker/.env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 256M
